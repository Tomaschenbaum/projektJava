<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ukasz.projektjavaandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DziennikActivity"></activity>
        <activity android:name=".BrzuchActivity" />
        <activity android:name=".NogiPosladkiActivity" />
        <activity android:name=".TricepsActivity" />
        <activity android:name=".BicepsActivity" />
        <activity android:name=".BarkiActivity" />
        <activity android:name=".PlecyActivity" />
        <activity android:name=".KlatkaPiersiowaActivity" />
        <activity
            android:name=".AtlasActivity"
            android:label="@string/title_activity_atlas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoggedInActivity" />
    </application>

</manifest>